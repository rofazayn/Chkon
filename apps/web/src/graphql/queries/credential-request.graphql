query CredentialRequest($where: CredentialRequestWhereUniqueInput!) {
  credentialRequest(where: $where) {
    id
    userId
    issuerId
    credentialTypeId
    status
    createdAt
    updatedAt
  }
}
