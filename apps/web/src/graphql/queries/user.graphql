query User($where: UserWhereUniqueInput!) {
  user(where: $where) {
    id
    username
    did
    phoneNumber
    email
    name
    logoUrl
    status
    trustedOrganizationsIds
    createdAt
    updatedAt
  }
}
