query User($where: UserWhereUniqueInput!) {
  user(where: $where) {
    id
    username
    did
    phoneNumber
    email
    name
    logoUrl
    status
    deleted
    trustedOrganizationsIds
    createdAt
    updatedAt
  }
}
