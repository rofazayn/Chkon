mutation Register(
  $username: String!
  $password: String!
  $email: String!
  $name: String!
) {
  register(
    username: $username
    password: $password
    email: $email
    name: $name
  ) {
    accessToken
    refreshToken
  }
}
